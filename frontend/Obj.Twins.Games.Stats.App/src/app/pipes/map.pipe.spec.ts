/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MapPipe } from './map.pipe';

describe('Pipe: Mape', () => {
  it('create an instance', () => {
    let pipe = new MapPipe();
    expect(pipe).toBeTruthy();
  });
});
